import { use } from "react";
import { ServiceProvider } from "@core/providers.js";

export const Login = () => {
  const system = use(ServiceProvider);

  return (
    <form>
      
    </form>
  );
};
